/*
  This script will fill out the language menu elements on the page.
  It will check to see what languages can be implemented
  and then change the default code in the menu
*/

var menu;

menu = document.getElementsByClassName('language-menu');

for (var i = 0; i < menu.length; i++) {
  // menu[i].innerHTML = '';
}
